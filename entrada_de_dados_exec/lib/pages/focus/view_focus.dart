import 'package:flutter/material.dart';

class Focused extends StatelessWidget {
  const Focused({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Focus'),
    );
  }
}
